version: 1
frontend:
  phases:
    build:
      commands: []
  artifacts:
    # Static site: no build step, publish the repo root as-is.
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
